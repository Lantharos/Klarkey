<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="KlarkeyPasskeyProvider.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            x:Uid="WindowTitleBar">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <ScrollViewer
            Grid.Row="1"
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto">
            <StackPanel
                Spacing="20"
                Padding="28,24,28,32">
                <StackPanel Spacing="6">
                    <TextBlock
                        x:Uid="MainHeading"
                        Style="{ThemeResource TitleTextBlockStyle}" />
                    <TextBlock
                        x:Uid="MainBody"
                        MaxWidth="720"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        TextWrapping="Wrap" />
                </StackPanel>

                <Grid
                    ColumnSpacing="12"
                    RowSpacing="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <TextBox
                        x:Uid="ElectronPathBox"
                        Grid.Row="0"
                        Grid.Column="0"
                        AutomationProperties.AutomationId="ElectronPathBox"
                        Text="{x:Bind ViewModel.ExecutablePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <TextBox
                        x:Uid="AppPathBox"
                        Grid.Row="1"
                        Grid.Column="0"
                        AutomationProperties.AutomationId="AppPathBox"
                        Text="{x:Bind ViewModel.AppPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                    <Button
                        x:Uid="PingBridgeButton"
                        Grid.Row="0"
                        Grid.RowSpan="2"
                        Grid.Column="1"
                        MinWidth="160"
                        AutomationProperties.AutomationId="PingBridgeButton"
                        Command="{x:Bind ViewModel.PingBridgeCommand, Mode=OneWay}" />
                </Grid>

                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <ProgressRing
                        Grid.Column="0"
                        Width="24"
                        Height="24"
                        IsActive="{x:Bind ViewModel.IsBusy, Mode=OneWay}" />

                    <StackPanel
                        Grid.Column="1"
                        Spacing="4">
                        <TextBlock
                            Text="{x:Bind ViewModel.StatusTitle, Mode=OneWay}"
                            Style="{ThemeResource SubtitleTextBlockStyle}" />
                        <TextBlock
                            Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}"
                            Style="{ThemeResource BodyTextBlockStyle}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            Text="{x:Bind ViewModel.BridgeDetail, Mode=OneWay}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Style="{ThemeResource BodyTextBlockStyle}"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
